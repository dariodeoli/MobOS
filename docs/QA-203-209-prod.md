# Verificación post-deploy #203 / #209 — Clientes, Servicio y portal

Producción **v1.0.132** (= `main`), corrida headless contra el demo público y
`clientes.moboss.online`. Capturas en `docs/QA-203-209-prod/`.

## #203 comprobante de verificación de IMEI — ✅ desplegado y verificado

| Paso | Resultado | Captura |
|---|---|---|
| Demo Dueño (se cierra el onboarding “Cómo funciona la demo”) | ✅ | — |
| Ficha de Lucía → Pedidos → **Verificación IMEI** | ✅ | `01-imei-demo-modal.png` |
| El modal simula: **“Verificado · Simulada en demo”**, IMEI enmascarado, “IMEI verificado: sin reportes al <fecha>”, **Fuente IMEIcheck.net** | ✅ | idem |
| **Adjuntar al comentario** (local en demo) | ✅ sin llamadas al API | `02-imei-demo-adjuntar.png` |
| **Imprimir**: avisa que en demo no se imprime con datos ficticios | ✅ | `03-imei-demo-imprimir.png` |
| **Cero llamadas** a `/api/imei`, `/api/customers`, `/api/message-templates` durante el recorrido | ✅ (`apiReal: []`) | — |

## Servicio y portal (contexto)

| Punto | Resultado | Captura |
|---|---|---|
| Servicio Técnico demo con OS-#0001 y OS-#0002 | ✅ | `05-servicio-demo.png` |
| Portal demo por token (`/cuenta/demo-…-completo`) con Tienda demo y saldo Gs 1.500.000 | ✅ | `04-portal-demo.png` |

## #209 “último usado como predeterminado” — helper desplegado, adopción CRM pendiente

- El **helper compartido** (`src/lib/ultimoUsado.js`, demo-safe con
  `demoStorage`, documentado en `docs/PLANTILLA-OBJETOS.md §7`) ya está en
  producción y lo usan FIN/INV/otros dominios.
- La **adopción en CRM** (plantilla de WhatsApp por contexto + filtro/orden de
  Clientes + estado de Garantías) viaja en esta rama (`d70c7d3`) y se verificó
  con e2e local: filtros que persisten al recargar y plantilla recordada al
  cerrar/reabrir el menú. Se re-verifica en producción cuando se deploye.

## Hallazgos

Ninguno de producto. La primera sonda falló por un artefacto de timing propio
(el modal se abría antes de que el efecto leyera el equipo); repetida con
espera explícita, todo verde.
