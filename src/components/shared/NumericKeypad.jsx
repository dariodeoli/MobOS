// Puente de migración (#253): la implementación vive en `owncoding-ui` (usa el
// Icon de la biblioteca para borrar; ya no depende de lucide-react) y acá solo
// queda la ruta histórica. No volver a implementar.
export { NumericKeypad as default } from 'owncoding-ui'
